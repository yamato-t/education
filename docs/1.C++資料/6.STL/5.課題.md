# **課題**

---
## **課題①　下記の要件を満たすプログラムを作成してください**

- int 型の vector コンテナを宣言し、１～１０までの整数値を追加してください。
- 次に、コンテナから偶数だけを削除してください。
- コンテナに残った数（奇数）をコンソールに全部表示してください


---
## **課題②　？を埋めてください**

下記のプログラムを実行すると、  
コンソールにはこのように表示される。

```
0 は 味方です
1 は 敵です
2 は 味方です
3 は 敵です
4 は 敵です
```

？の部分をコーディングしてください。


```c++
#include <iostream>
#include <map>

// キャラクター基礎
class CharacterBase
{
public:
	// コンストラクタ
	CharacterBase() = default;
	// デストラクタ
	virtual ~CharacterBase() = default;

public:
    ？
};


// プレイヤー
class Player : public CharacterBase
{
public:
	// コンストラクタ
	Player() = default;
	// デストラクタ
	virtual ~Player() = default;

public:
	const char* type() ？ {
		return "味方です";
	}
};

// 敵
class Enemy : public CharacterBase
{
public:
	// コンストラクタ
	Enemy() = default;
	// デストラクタ
	virtual ~Enemy() = default;

public:
	const char* type() ？ {
		return "敵です";
	}
};


// エントリー関数
int main()
{
	// コンテナを埋める
	std::map<int, CharacterBase*> map;
	map.emplace(0, ？);
	map.emplace(1, ？);
	map.emplace(2, ？);
	map.emplace(3, ？);
	map.emplace(4, ？);

	// 種類を表示する
	for (std::map<int, CharacterBase*>::iterator it = map.begin(); it != map.end(); ++it)
	{
		std::cout << it->first << " は " << it->second->type() << std::endl;
	}

	// コンテナ中身のインスタンス削除も忘れずにする
    ？

    // コンテナをすべて破棄する
	map.clear();
}

```

---
## **課題③　myFind 関数の内容を考えてください**

下記は ```ClassA``` と ```ClassB``` の各コンテナから、  
```type_``` が 「1」の物を検索する処理がコーディングされている。

この時 ```myFind``` 関数はどの様にコーディングすればいいか考えてください。  
ただし、**既存のコードを変更してはいけません**。  
```ClassA``` と ```ClassB``` の内容の変更も禁止します。  

いくつかのグローバル関数を新規に追加するだけで実現する方法を考えてください。


```c++
#include <iostream>
#include <vector>
#include <algorithm>

// クラスA
class ClassA
{
public:
	// コンストラクタ
	ClassA(int type) :type_(type) {}

public:
	// タイプ取得
	int type() const { return type_; }

private:
	int type_ = 0;
};

// クラスB
class ClassB
{
public:
	// コンストラクタ
	ClassB(int type) :type_(type) {}

public:
	// タイプ取得
	int type() const { return type_; }

private:
	int type_ = 0;
};


// エントリー関数
int main()
{
	// クラスAのベクタ
	std::vector<ClassA> aVector;
	aVector.emplace_back(1);
	aVector.emplace_back(2);
	aVector.emplace_back(0);
	aVector.emplace_back(3);


	// クラスBのベクタ
	std::vector<ClassB> bVector;
	bVector.emplace_back(0);
	bVector.emplace_back(2);
	bVector.emplace_back(1);
	bVector.emplace_back(3);

	bool findA = myFind(aVector);
	if (findA)
	{
		std::cout << "aVector に 「1」 があった" << std::endl;
	}

	bool findb = myFind(bVector);
	if (findb)
	{
		std::cout << "bVector に 「1」 があった" << std::endl;
	}

}
```