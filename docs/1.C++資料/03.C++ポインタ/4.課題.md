# **課題**

---
## **課題①　クラスを定義し unique_ptr でインスタンス化してください**

`std::unique_ptr` の基本動作を確認するプログラムを作成してください。

- クラスのコンストラクタで「生成」と表示される様にする
- クラスのデストラクタで「破棄」と表示される様にする
- スコープ内で `std::make_unique` でインスタンスを生成する

---
## **課題②　shared_ptr で循環参照するプログラムを作成してください**

`shared_ptr` を使ってわざと循環参照するプログラムを作成してください

- 課題①で作成したクラスを改造して利用する
- 二つのインスタンスでお互いの `shared_ptr` を保持させる
- スコープ終了時にデストラクタが呼ばれない事が確認できれば OK


---
## **課題③　課題②を改造して循環参照を回避するプログラムを作成してください**

循環参照対策を行ったプログラムを作成してください


---
## **課題③　nullptr とオーバーロードの問題解決を確認するプログラムを作成してください**

`NULL` ではなく `nullptr` を使う利点を確認してください。

- `func(int)` と `func(char*)` をオーバーロードする
- `nullptr` を渡した時にどちらが呼ばれるか確認する
- `NULL` を渡した場合の曖昧さをコメントで説明する
- ポインタ引数を受ける関数で `nullptr` チェックを実装する

---
