# **デプスステンシル**

---

カメラとオブジェクトのコンスタントバッファを個別に分けて、  
描画処理（GPU）で利用する所まで進んだ。  
同じ方法で「オブジェクトを複数描画する」という事が可能になる。

ただ複数のオブジェクトを 3D 描画する場合、  

- オブジェクト同士の描画優先（どのオブジェクトを最前面で描くか）
- 重なった時にどの様に描画するか（べた塗 or 色を重ねる？）

など、考慮しなければならない事が増える。  

それらの設定（ステート）は総称して<span style="color: red;">**パイプラインステート（レンダーステート）**</span>と呼ばれ、  
この設定を工夫する事で様々な描画が可能になる。

この項目で説明する<span style="color: red;">**デプスステンシル**</span>もその一つとなる。

## **デプスバッファ（深度バッファ）**

デプスバッファは3D描画において最も重要な要素の一つ。  
オブジェクトの前後関係を正確に判定し、  
手前にあるオブジェクトが奥のオブジェクトを隠すという、  
自然な描画結果を実現する。  

### **デプスバッファの仕組み**

デプスバッファはピクセルごとにカメラからの距離（深度値）を記録する。  
新しいピクセルを描画する際、既存の深度値と比較し、  
より手前にある場合のみピクセルを更新する。  
この処理により、描画順序に依存せず正確な前後関係を表現できる。  

これを**デプス（深度 あるいは Z）テスト**と呼ぶ。

深度値は通常0.0（ニアクリップ面）から1.0（ファークリップ面）の範囲で表現され、  
カメラに近いほど小さな値となる。  

### **デプスバッファリソースの作成**

デプスバッファは専用のテクスチャリソースとして作成する。  
RenderTarget、つまり画用紙と同じ部類。  

フォーマットは DXGI_FORMAT_D32_FLOATやDXGI_FORMAT_D24_UNORM_S8_UINT を使用する。  

```c++
// デプスバッファ用のテクスチャリソースの作成
D3D12_HEAP_PROPERTIES heapProps = {};
heapProps.Type = D3D12_HEAP_TYPE_DEFAULT;
D3D12_RESOURCE_DESC depthDesc = {};
depthDesc.Dimension = D3D12_RESOURCE_DIMENSION_TEXTURE2D;
depthDesc.Width = width;
depthDesc.Height = height;
depthDesc.DepthOrArraySize = 1;
depthDesc.MipLevels = 1;
depthDesc.Format = DXGI_FORMAT_D32_FLOAT;
depthDesc.SampleDesc.Count = 1;
depthDesc.SampleDesc.Quality = 0;
depthDesc.Layout = D3D12_TEXTURE_LAYOUT_UNKNOWN;
depthDesc.Flags = D3D12_RESOURCE_FLAG_ALLOW_DEPTH_STENCIL;

// デプスバッファの初期値
D3D12_CLEAR_VALUE clearValue = {};
clearValue.Format = depthDesc.Format;
clearValue.DepthStencil.Depth = 1.0f;
clearValue.DepthStencil.Stencil = 0;


ID3D12Resource* depthBuffer;
device->CreateCommittedResource(
    &heapProps,
    D3D12_HEAP_FLAG_NONE,
    &depthDesc,
    D3D12_RESOURCE_STATE_DEPTH_WRITE,
    &clearValue,
    IID_PPV_ARGS(&depthBuffer)
);
```

### **デプスビューの作成**

作成したデプスバッファをパイプラインで使用するには、  
DSV（Depth Stencil View）を作成する必要がある。  

DSV はディスクリプタヒープも専用の物を利用する。

```c++
// DSV用ディスクリプタヒープの作成
D3D12_DESCRIPTOR_HEAP_DESC dsvHeapDesc = {};
dsvHeapDesc.Type = D3D12_DESCRIPTOR_HEAP_TYPE_DSV;
dsvHeapDesc.NumDescriptors = 1;
dsvHeapDesc.Flags = D3D12_DESCRIPTOR_HEAP_FLAG_NONE;

ID3D12DescriptorHeap* dsvHeap;
device->CreateDescriptorHeap(&dsvHeapDesc, IID_PPV_ARGS(&dsvHeap));

// DSVの作成
D3D12_DEPTH_STENCIL_VIEW_DESC dsvDesc = {};
dsvDesc.Format = DXGI_FORMAT_D32_FLOAT;
dsvDesc.ViewDimension = D3D12_DSV_DIMENSION_TEXTURE2D;
dsvDesc.Flags = D3D12_DSV_FLAG_NONE;

D3D12_CPU_DESCRIPTOR_HANDLE dsvHandle = 
    dsvHeap->GetCPUDescriptorHandleForHeapStart();
device->CreateDepthStencilView(
    depthBuffer, 
    &dsvDesc, 
    dsvHandle
);
```

### **デプスステートの設定**

パイプラインステートオブジェクト（PSO）で深度テストの動作内容を設定する。  

```c++
// デプスステートの設定
D3D12_DEPTH_STENCIL_DESC depthStateDesc = {};
depthStateDesc.DepthEnable = true;
depthStateDesc.StencilEnable = false;
depthStateDesc.DepthWriteMask = D3D12_DEPTH_WRITE_MASK_ALL;
depthStateDesc.DepthFunc = D3D12_COMPARISON_FUNC_LESS;

// PSOに設定
psoDesc.DepthStencilState = depthStateDesc;
psoDesc.DSVFormat = DXGI_FORMAT_D32_FLOAT;
```

### **深度比較関数の種類**

DepthFuncで指定できる比較関数は以下の通り。  

| 比較関数 | 説明 | 用途 |
|---------|------|------|
| NEVER | 常に描画しない | デバッグ用 |
| LESS | 既存値より小さい場合描画 | 通常の3D描画 |
| EQUAL | 既存値と等しい場合描画 | 特殊な用途 |
| LESS_EQUAL | 既存値以下の場合描画 | デカール等 |
| GREATER | 既存値より大きい場合描画 | 逆深度バッファ |
| NOT_EQUAL | 既存値と異なる場合描画 | 特殊な用途 |
| GREATER_EQUAL | 既存値以上の場合描画 | 逆深度バッファ |
| ALWAYS | 常に描画する | デプステスト無効化 |

### **レンダーターゲットとデプスバッファの設定**

描画コマンド実行前に、レンダーターゲットとデプスバッファを設定する。  

```c++
// レンダーターゲットとデプスの設定
D3D12_CPU_DESCRIPTOR_HANDLE rtvHandle = 
    rtvHeap->GetCPUDescriptorHandleForHeapStart();
D3D12_CPU_DESCRIPTOR_HANDLE dsvHandle = 
    dsvHeap->GetCPUDescriptorHandleForHeapStart();

commandList->OMSetRenderTargets(1, &rtvHandle, FALSE, &dsvHandle);
```

### **デプスバッファのクリア**

フレームの開始時にデプスバッファを初期化する必要がある。  

```c++
// デプスバッファのクリア
commandList->ClearDepthStencilView(
    dsvHandle,
    D3D12_CLEAR_FLAG_DEPTH,
    1.0f,  // クリア値（最大深度）
    0,     // ステンシルクリア値
    0,
    nullptr
);
```

### **デプスフォーマットの選択**

デプスバッファのフォーマットは精度と用途に応じて選択する。  

| フォーマット | 深度ビット数 | ステンシルビット数（※） | 用途 |
|-------------|-------------|-------------------|------|
| DXGI_FORMAT_D32_FLOAT | 32 | 0 | 高精度深度 |
| DXGI_FORMAT_D24_UNORM_S8_UINT | 24 | 8 | 深度+ステンシル |
| DXGI_FORMAT_D16_UNORM | 16 | 0 | 低メモリ消費 |
| DXGI_FORMAT_D32_FLOAT_S8X24_UINT | 32 | 8 | 最高精度 |



### **デプスバッファを使用しない場合**

デプスバッファを使用しない場合、描画の順番によって前後関係が決定される。  

```c++
// デプステスト無効化の例
depthStateDesc.DepthEnable = false;
```

この設定では、後から描画されたオブジェクトが常に手前に表示されるため、  
3D空間の自然な表現ができなくなる。  
複数のオブジェクトが重なる場合、正確な前後関係が表現できない。  

## **ステンシル**

デプスバッファは「ステンシル」と呼ばれる情報と合わせて利用される事が多い。  
ステンシルとは、各ピクセルごとに「印」を記録できる仕組みを指す。

印を記録する事で、  

- 一度目の描画で「印」をピクセルに記録する
- 二度目の描画で「特定の印」が記録されたピクセルだけ重ね描きする

といった複雑な描画制御が可能になる。  
たとえば、

- この範囲だけに影を落とす
- 穴あきの範囲にだけ色を塗る

といった表現に利用できる。

### **ステンシルの設定コード**

ステンシルを使用するには、デプスバッファのフォーマットに  
ステンシルビットを持つものを選ぶ必要がある。  

```c++
// ステンシル付きフォーマット（24bit深度 + 8bitステンシル）に変更
depthDesc.Format  = DXGI_FORMAT_D24_UNORM_S8_UINT;
clearValue.Format = DXGI_FORMAT_D24_UNORM_S8_UINT;
```

**1回目の描画 — マスク領域に「印」を書き込む**

```c++
D3D12_DEPTH_STENCIL_DESC writeDesc = {};
writeDesc.DepthEnable    = TRUE;
writeDesc.DepthWriteMask = D3D12_DEPTH_WRITE_MASK_ALL;
writeDesc.DepthFunc      = D3D12_COMPARISON_FUNC_LESS;
writeDesc.StencilEnable  = TRUE;       // ステンシルを有効化
writeDesc.StencilReadMask  = 0xFF;
writeDesc.StencilWriteMask = 0xFF;

writeDesc.FrontFace.StencilFailOp      = D3D12_STENCIL_OP_KEEP;
writeDesc.FrontFace.StencilDepthFailOp = D3D12_STENCIL_OP_KEEP;
writeDesc.FrontFace.StencilPassOp      = D3D12_STENCIL_OP_REPLACE; // 参照値を書き込む
writeDesc.FrontFace.StencilFunc        = D3D12_COMPARISON_FUNC_ALWAYS;
writeDesc.BackFace = writeDesc.FrontFace;

psoDesc.DepthStencilState = writeDesc;

// 参照値（「印」として書き込む値）
commandList->OMSetStencilRef(1);
```

**2回目の描画 — 「印」があるピクセルだけ描画する**

```c++
D3D12_DEPTH_STENCIL_DESC readDesc = {};
readDesc.DepthEnable    = TRUE;
readDesc.DepthWriteMask = D3D12_DEPTH_WRITE_MASK_ZERO;
readDesc.DepthFunc      = D3D12_COMPARISON_FUNC_LESS;
readDesc.StencilEnable  = TRUE;
readDesc.StencilReadMask  = 0xFF;
readDesc.StencilWriteMask = 0xFF;

readDesc.FrontFace.StencilFailOp      = D3D12_STENCIL_OP_KEEP;
readDesc.FrontFace.StencilDepthFailOp = D3D12_STENCIL_OP_KEEP;
readDesc.FrontFace.StencilPassOp      = D3D12_STENCIL_OP_KEEP;
readDesc.FrontFace.StencilFunc        = D3D12_COMPARISON_FUNC_EQUAL; // 印と一致する場所のみ
readDesc.BackFace = readDesc.FrontFace;

psoDesc.DepthStencilState = readDesc;
commandList->OMSetStencilRef(1); // 1回目と同じ参照値で比較
```

ステンシルバッファは深度と同時にクリアする。  

```c++
commandList->ClearDepthStencilView(
    dsvHandle,
    D3D12_CLEAR_FLAG_DEPTH | D3D12_CLEAR_FLAG_STENCIL, // 両方クリア
    1.0f,
    0,       // ステンシルの初期値
    0, nullptr
);
```

---


